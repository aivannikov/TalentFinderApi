{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "lint": "eslint .",
    "start": "node -r dotenv/config app.js dotenv_config_path=.env.dev",
    "test": "mocha test/**/*.spec.js -r dotenv/config --timeout 10000 dotenv_config_path=.env.test"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-http": "^4.3.0",
    "dotenv": "^8.2.0",
    "mocha": "^8.1.1"
  }
}
